[package]
name = "graph-clonable-ref"
version = "0.1.0"
edition = "2021"

[dependencies]

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[[bench]]
name = "deep_clone_benchmark"
harness = false
